const clearContent = () => {
  const contentContainer = document.querySelector(".content");
  // need to clear the content display
  console.log("clear content is being called");
  // TODO: Clear the main content display
};

export default clearContent();
